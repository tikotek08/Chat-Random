services:
  - type: web
    name: chat-random-signal
    env: node
    plan: free
    rootDir: signaling-server
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production

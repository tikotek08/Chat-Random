services:
  - type: web
    name: chat-random-signal
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server/signaling.mjs
    envVars:
      - key: NODE_ENV
        value: production
